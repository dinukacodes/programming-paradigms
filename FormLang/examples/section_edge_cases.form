form SectionEdgeCases {
    section EmptySection {
    }

    section SingleFieldSection {
        field test: text required;
    }

    section UnicodeSectionName {
        field test: text required;
    }

    section SectionWithSpecialChars {
        field test: text required;
    }

    section VeryLongSectionNameThatExceedsNormalLengthButShouldStillWorkProperly {
        field test: text required;
    }

    section MultipleEmptySections {
    }

    section AnotherEmptySection {
    }

    section FinalSectionWithAllFieldTypes {
        field text_field: text required;
        field email_field: email required;
        field password_field: password required;
        field number_field: number required;
        field textarea_field: textarea required;
        field date_field: date required;
        field checkbox_field: checkbox required;
        field radio_field: radio required;
        field dropdown_field: dropdown required;
        field file_field: file required;
    }
} 